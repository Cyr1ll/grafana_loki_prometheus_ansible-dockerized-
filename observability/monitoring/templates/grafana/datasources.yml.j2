apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:{{ monitoring__prometheus_port }}
    isDefault: true
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:{{ monitoring__loki_port }}
    isDefault: false
